#include <stdio.h>
#include <string.h>

int main()
{
    char all[400];
    char nju[20][20];
    int i, j, k;
    i = j = k = 0;

    printf("wpisz tekst: ");
    gets(all);

    for (i = 0; i < 20; i++)
    {
        for (j = 0; j < 20; j++)
        {
            for (; k < 400; k++)
            {
                nju[i][j] = all[k];
                if ((int)all[k] == 32)
                {
                    break;
                }
            }
        }
    }
    
    for(i=0; i<20;i++)
   {
     puts(nju[i]);  
   }
   

    return 0;
}
