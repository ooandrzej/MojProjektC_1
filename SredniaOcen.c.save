#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

struct oceny
{
	char przedmiot;
	int ocena;
};

int main()
{
	char przedmiot[15][12] = {
		"Biologia",
		"Chemia",
		"J.angielski",
		"Matematyka",
		"J.polski",
		"J.rosyjski",
		"Hitoria",
		"WOS",
		"Informatyka",
		"W-F",
		"Religia",
		"EDB",
		"Fizyka",
		"Filozofia",
		"Geografia",
	};
	int i, j, p, o, nroceny;
	char pu = ' ';
	int ocena[15][12];

	for (i = 0; i < 15; i++)
	{
		for (j = 0; j < 12; j++)
			ocena[i][j] = 0;
		{
		}
	}

	for (p = 0; p < 15; p++) //pętla
	{
		for (o = 0; o < 12; o++)
		{
			ocena[p][o] = 0;
		}
	}

	for (p = 0; p < 15; p++) //pętla
	{
		for (o = 0; o < 12; o++)
		{
			printf("Ocene nr_%d z przedmiotu %s (kolejna [Enter], kolejny przedmiot[ 0 Enter]): ", o + 1, przedmiot[p]);
			scanf("%d", &ocena[p][o]);
			printf("%d ", ocena[p][o]);
			if (ocena[p][o] < 1)
				break;
		}
	}

	for (p = 0; p < 15; p++) //pętla
	{
		printf("\n");
		for (o = 0; o < 12; o++)
		{
			printf("%s : %d ", przedmiot[p], ((ocena[p][o]) == 0) ? (ocena[p][o]) : pu);
		}
	}

	return 0;
}
